{
  "name": "friendzy-mobile",
  "version": "1.0.0",
  "description": "Friendzy Mobile App",
  "private": true,
  "scripts": {
    "sync": "npx cap sync",
    "build": "cd ../frontend && npm run build && npx cap sync",
    "android": "npx cap open android",
    "ios": "npx cap open ios",
    "dev:android": "npx cap run android",
    "dev:ios": "npx cap run ios"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/app": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor/haptics": "^6.2.0",
    "@capacitor/keyboard": "^6.2.0",
    "@capacitor/push-notifications": "^6.2.0",
    "@capacitor/share": "^6.2.0",
    "@capacitor/splash-screen": "^6.2.0",
    "@capacitor/status-bar": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
